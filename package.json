{
  "name": "aa-mahjong",
  "private": true,
  "version": "1.4.53",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run verify:cutouts && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "verify:cutouts": "node scripts/verify-cutouts.mjs",
    "build:play": "npm run verify:cutouts && vite build --mode play && node scripts/fix-play-html.mjs"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wrangler": "^4.132.0"
  }
}
